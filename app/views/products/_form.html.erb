<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :catagory_id %><br>
    <%= f.collection_select :catagory_id, Catagory.all, :id, :name  %>
  </div>
  <div class="field">
    <%= f.label :unit%><br>
    <%= f.text_field :unit%>
  </div>
  <div class="field">
    <%= f.label :version_id %><br>
    <%= f.collection_select :version_id, Version.all, :id, :id%>
  </div>

  <div class="field">
    <%= f.label :isNew %><br>
    <%= f.text_field :isNew %>
  </div>
  <div class="field">
    <%= f.label :isHot %><br>
    <%= f.text_field :isHot%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
