<%= form_for(@version) do |f| %>
  <% if @version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@version.errors.count, "error") %> prohibited this version from being saved:</h2>

      <ul>
      <% @version.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :validate_from %><br>
    <%= f.text_field :validate_from %>
  </div>
  <div class="field">
    <%= f.label :validate_to %><br>
    <%= f.label :validate_to %>
  </div>
  <div class="field">
    <%= f.label :isValid %><br>
    <%= f.text_field :isValid %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
